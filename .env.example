# =============================================================================
# PRIVACY NOTICE
# =============================================================================
# This is a therapy application. All telemetry is disabled in code.
# For maximum privacy, consider:
# - Self-hosted LLMs via LLM_BASE_URL (Ollama, LM Studio)
# - OpenAI Zero Data Retention (ZDR) for enterprise
# - Review your LLM provider's data retention policy
# =============================================================================

# Environment
ENVIRONMENT=development

# Telemetry disabled (also enforced in code)
ANONYMIZED_TELEMETRY=False

# API Keys
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# RECOMMENDED FOR PRIVACY: Use a local LLM instead of cloud APIs
# LLM_BASE_URL=http://localhost:1234/v1  # LM Studio
# LLM_BASE_URL=http://localhost:11434/v1 # Ollama

# Model Configuration
# Main Model: Chat persona + one side of Aegean consensus
LLM_MAIN_MODEL=gpt-4o

# Reviewer Model: Other side of Aegean consensus (should differ from main)
LLM_REVIEWER_MODEL=claude-3-5-sonnet

# Database Configuration
# For local development with SQLite (default):
DATABASE_URL=sqlite:///./data/stoic_emperor.db
# For PostgreSQL (production):
# DATABASE_URL=postgresql://user:password@localhost:5432/stoic_emperor
# For Supabase:
# DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# Paths
STOIC_TEXTS_FOLDER=./data/stoic_texts

# Authentication
# JWT secret for local auth (auto-generated if not set)
# JWT_SECRET=your-secret-here
